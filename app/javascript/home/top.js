import Modal from "../custom/modal";

const createModal = new Modal({
  modal: "modal",
  openButton: "create",
  className: "create",
});
const loginModal = new Modal({
  modal: "modal",
  openButton: "login",
  className: "login",
});
